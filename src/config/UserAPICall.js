export const getAllWatchList = user =>{
    // an api call to the server to fetch all the eatchLists of tht secific user
    return []
}

export const getAllStocks = (user,watchList) => {
    // an api call to the server to fetch all the stock list for that specific user's wishList
    return [];
}
